git status - it will check the ststus of the git repository

git init - it will intialize the empty repo

git add <filename> - adding the file to the git repo

git commit -m "comment1 - " - it commits the code in the git repo


git config --global user.name ukeathar

git config --global user.email udaykiran.eathar@gmail.com
 
git config --global --list

git remote add origin https://github.com/ukeathar/git-test-repo.git

git push -u origin master




